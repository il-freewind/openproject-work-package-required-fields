# TODO

- Exclude boolean fields from required-field rule configuration, including both standard boolean fields and boolean custom fields.
- Verify required-field validation for status changes made from work package lists, bulk status updates, and API-based updates.
